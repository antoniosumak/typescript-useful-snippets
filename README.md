###Where to create snippets
Position yourself into `./src` folder and create a `.ts` file

### How to run a script

Position yourself into `./src` folder and run next command:

```bash
npx ts-node filename.ts
```
